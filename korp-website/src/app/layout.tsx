import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "KORP Software | Complete Stock Market Back-Office Management Solution",
  description: "KORP Software provides complete back-office management solutions for stock market members including Capital Market, F&O Derivative, Currency Derivative, Commodity, Depository, and Mutual Fund management.",
  keywords: "stock market software, back office software, trading software, F&O, CDSL, NSDL, mutual fund software, risk management",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
